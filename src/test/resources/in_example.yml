openapi: 3.0.0
info:
  title: Test 9
  version: 1.0.0
paths:
  /product:
    get:
      responses:
        200:
          content:
            application/json:
              example:
                $ref: '#/components/examples/ProductExample'
components:
  examples:
    ProductExample:
      value:
        name: "Shoe"
        size: 42
