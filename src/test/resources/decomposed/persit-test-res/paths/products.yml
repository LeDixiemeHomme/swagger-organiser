get:
  summary: "Liste des produits"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../components/Product.yml"
    "400":
      description: "Erreur"
      content:
        application/json:
          schema:
            $ref: "../components/Error.yml"
