openapi: 3.0.0
info:
  title: Test 2
  version: 1.0.0
paths:
  /user:
    get:
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        200:
          description: OK
components:
  parameters:
    UserId:
      name: id
      in: query
      required: true
      schema:
        type: string
