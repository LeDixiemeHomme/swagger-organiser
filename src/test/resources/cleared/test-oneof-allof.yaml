openapi: 3.0.0
info:
  title: Test OneOf & AllOf
  version: 1.0.0
paths:
  /example:
    get:
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Object1'
components:
  schemas:
    Object1:
      oneOf:
        - $ref: '#/components/schemas/Object2'
        - $ref: '#/components/schemas/Object3'

    Object2:
      allOf:
        - $ref: '#/components/schemas/Object3'
        - $ref: '#/components/schemas/Object4'

    Object3:
      type: object
      properties:
        fieldA:
          type: string

    Object4:
      type: object
      properties:
        fieldB:
          type: integer
